BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[RewardImg] ADD [RewardImgIndex] BIT CONSTRAINT [RewardImg_RewardImgIndex_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
